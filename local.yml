- hosts: localhost
  connection: local
  brcome: true

  pre_tasks:
    - name: update cache
      package:
        update_cache: yes
      changed_when: false

  tasks:
    - include: tasks/packages.yml

      