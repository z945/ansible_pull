---

- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: update cache
      package:
        update_cache: yes
      changed_when: false

  tasks:
    - include: tasks/packages.yml
    - include: tasks/cron.yml
    - include: tasks/users.yml